  <nav class="navbar navbar-expand-lg">
    <div class="container">
      <div class="col-md-4 col-2">
        <% if user_signed_in? %>
          <%= image_tag current_user.image, width: "40px" %>
        <% end %>
      </div>
      <div class="col-md-4 col-8 text-center">
        <%= image_tag "service_title.png", width: "210px" %>
      </div>
      <div class="col-md-4 col-2 text-right">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse float-right" id="navbarNav">
          <ul class="navbar-nav">
            <% if user_signed_in? %>
              <li class="nav-item">
                <%= link_to "新着の評定", edit_book_path(@book), class: "nav-link pl-0" %>
              </li>
              <li class="nav-item">
                <%= link_to "友達の通信簿", "#", class: "nav-link pl-0" %>
              </li>
              <li class="nav-item">
                <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "nav-link pl-0" %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "Twitterでログイン", omniauth_authorize_path(:user, :twitter), class: "nav-link" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </nav>